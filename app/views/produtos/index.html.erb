<% @produtos.each do |produto| %>
  <div id="<%= dom_id produto %>">
    <p>
      <strong>Nome:</strong>
      <%= produto.nome %>
    </p>

    <% if produto.imagem.attached? %>
      <%= image_tag produto.imagem, style: "width: 200px; height: auto;" %>
    <% end %>

    <p>
      <%= link_to "Show this produto", produto %>
    </p>
  </div>
<% end %>